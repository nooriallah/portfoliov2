// Superseded by SectionDepth.jsx, which adds the drifting accent aura.
// Kept only as a re-export so nothing breaks if an old import survives
// somewhere; safe to delete.
export { default } from "./SectionDepth.jsx";
